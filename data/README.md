# Restricted source data

The complete participant-level workbook is not stored in this public repository. Its quasi-identifiers and free-text fields create a material re-identification risk. Authorized investigators should supply its path with `--data` when running `analysis/run_all.py`.

The PLOS ONE Supporting Information contains the de-identified release. Its variable dictionary is copied to `docs/variable_dictionary.csv` in this repository.
